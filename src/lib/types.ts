export type DataObj = {
  date: string;
  title: string;
  company: string;
  location: string;
  salary: string;
  description: string;
  platform: string;
};

export type ProfileObj = {
  date: string;
  name: string;
  title: string;
  company: string;
  link: string;
  connectionDegree: string;
};
